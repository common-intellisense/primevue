import Accordion from './Accordion.json'
import AccordionContent from './AccordionContent.json'
import AccordionHeader from './AccordionHeader.json'
import AccordionPanel from './AccordionPanel.json'
import AccordionTab from './AccordionTab.json'
import AutoComplete from './AutoComplete.json'
import Avatar from './Avatar.json'
import AvatarGroup from './AvatarGroup.json'
import Badge from './Badge.json'
import BlockUI from './BlockUI.json'
import Breadcrumb from './Breadcrumb.json'
import Button from './Button.json'
import ButtonGroup from './ButtonGroup.json'
import Calendar from './Calendar.json'
import Card from './Card.json'
import Carousel from './Carousel.json'
import CascadeSelect from './CascadeSelect.json'
import Chart from './Chart.json'
import Checkbox from './Checkbox.json'
import Chip from './Chip.json'
import Chips from './Chips.json'
import ColorPicker from './ColorPicker.json'
import Column from './Column.json'
import ColumnGroup from './ColumnGroup.json'
import ConfirmDialog from './ConfirmDialog.json'
import ConfirmPopup from './ConfirmPopup.json'
import ContextMenu from './ContextMenu.json'
import DataTable from './DataTable.json'
import DataView from './DataView.json'
import DatePicker from './DatePicker.json'
import DeferredContent from './DeferredContent.json'
import Dialog from './Dialog.json'
import Divider from './Divider.json'
import Dock from './Dock.json'
import Drawer from './Drawer.json'
import Dropdown from './Dropdown.json'
import DynamicDialog from './DynamicDialog.json'
import Editor from './Editor.json'
import Fieldset from './Fieldset.json'
import FileUpload from './FileUpload.json'
import FloatLabel from './FloatLabel.json'
import Fluid from './Fluid.json'
import Galleria from './Galleria.json'
import IconField from './IconField.json'
import IftaLabel from './IftaLabel.json'
import Image from './Image.json'
import ImageCompare from './ImageCompare.json'
import InlineMessage from './InlineMessage.json'
import Inplace from './Inplace.json'
import InputChips from './InputChips.json'
import InputGroup from './InputGroup.json'
import InputGroupAddon from './InputGroupAddon.json'
import InputIcon from './InputIcon.json'
import InputMask from './InputMask.json'
import InputNumber from './InputNumber.json'
import InputOtp from './InputOtp.json'
import InputSwitch from './InputSwitch.json'
import InputText from './InputText.json'
import Knob from './Knob.json'
import ListBox from './ListBox.json'
import MegaMenu from './MegaMenu.json'
import Menu from './Menu.json'
import Menubar from './Menubar.json'
import Message from './Message.json'
import MeterGroup from './MeterGroup.json'
import MultiSelect from './MultiSelect.json'
import OrderList from './OrderList.json'
import OrganizationChart from './OrganizationChart.json'
import OverlayBadge from './OverlayBadge.json'
import OverlayPanel from './OverlayPanel.json'
import Paginator from './Paginator.json'
import Panel from './Panel.json'
import PanelMenu from './PanelMenu.json'
import Password from './Password.json'
import PickList from './PickList.json'
import Popover from './Popover.json'
import ProgressBar from './ProgressBar.json'
import ProgressSpinner from './ProgressSpinner.json'
import RadioButton from './RadioButton.json'
import Rating from './Rating.json'
import Row from './Row.json'
import ScrollPanel from './ScrollPanel.json'
import ScrollTop from './ScrollTop.json'
import Select from './Select.json'
import SelectButton from './SelectButton.json'
import Sidebar from './Sidebar.json'
import Skeleton from './Skeleton.json'
import Slider from './Slider.json'
import SpeedDial from './SpeedDial.json'
import SplitButton from './SplitButton.json'
import Splitter from './Splitter.json'
import SplitterPanel from './SplitterPanel.json'
import Step from './Step.json'
import StepItem from './StepItem.json'
import StepList from './StepList.json'
import StepPanel from './StepPanel.json'
import StepPanels from './StepPanels.json'
import Stepper from './Stepper.json'
import Steps from './Steps.json'
import Tab from './Tab.json'
import TabList from './TabList.json'
import TabMenu from './TabMenu.json'
import TabPanel from './TabPanel.json'
import TabPanels from './TabPanels.json'
import Tabs from './Tabs.json'
import Tag from './Tag.json'
import Terminal from './Terminal.json'
import Textarea from './Textarea.json'
import TieredMenu from './TieredMenu.json'
import Timeline from './Timeline.json'
import Toast from './Toast.json'
import ToggleButton from './ToggleButton.json'
import ToggleSwitch from './ToggleSwitch.json'
import Toolbar from './Toolbar.json'
import Tree from './Tree.json'
import TreeSelect from './TreeSelect.json'
import TreeTable from './TreeTable.json'
import VirtualScroller from './VirtualScroller.json'

export function getPropsMap() {
  return [
    Accordion,
    AccordionContent,
    AccordionHeader,
    AccordionPanel,
    AccordionTab,
    AutoComplete,
    Avatar,
    AvatarGroup,
    Badge,
    BlockUI,
    Breadcrumb,
    Button,
    ButtonGroup,
    Calendar,
    Card,
    Carousel,
    CascadeSelect,
    Chart,
    Checkbox,
    Chip,
    Chips,
    ColorPicker,
    Column,
    ColumnGroup,
    ConfirmDialog,
    ConfirmPopup,
    ContextMenu,
    DataTable,
    DataView,
    DatePicker,
    DeferredContent,
    Dialog,
    Divider,
    Dock,
    Drawer,
    Dropdown,
    DynamicDialog,
    Editor,
    Fieldset,
    FileUpload,
    FloatLabel,
    Fluid,
    Galleria,
    IconField,
    IftaLabel,
    Image,
    ImageCompare,
    InlineMessage,
    Inplace,
    InputChips,
    InputGroup,
    InputGroupAddon,
    InputIcon,
    InputMask,
    InputNumber,
    InputOtp,
    InputSwitch,
    InputText,
    Knob,
    ListBox,
    MegaMenu,
    Menu,
    Menubar,
    Message,
    MeterGroup,
    MultiSelect,
    OrderList,
    OrganizationChart,
    OverlayBadge,
    OverlayPanel,
    Paginator,
    Panel,
    PanelMenu,
    Password,
    PickList,
    Popover,
    ProgressBar,
    ProgressSpinner,
    RadioButton,
    Rating,
    Row,
    ScrollPanel,
    ScrollTop,
    Select,
    SelectButton,
    Sidebar,
    Skeleton,
    Slider,
    SpeedDial,
    SplitButton,
    Splitter,
    SplitterPanel,
    Step,
    StepItem,
    StepList,
    StepPanel,
    StepPanels,
    Stepper,
    Steps,
    Tab,
    TabList,
    TabMenu,
    TabPanel,
    TabPanels,
    Tabs,
    Tag,
    Terminal,
    Textarea,
    TieredMenu,
    Timeline,
    Toast,
    ToggleButton,
    ToggleSwitch,
    Toolbar,
    Tree,
    TreeSelect,
    TreeTable,
    VirtualScroller,
  ]
}

export function getComponentsMap() {
  return [
    [Accordion, Accordion.name, `<${Accordion.name}></${Accordion.name}`],
    [AccordionContent, AccordionContent.name, `<${AccordionContent.name}></${AccordionContent.name}`],
    [AccordionHeader, AccordionHeader.name, `<${AccordionHeader.name}></${AccordionHeader.name}`],
    [AccordionPanel, AccordionPanel.name, `<${AccordionPanel.name}></${AccordionPanel.name}`],
    [AccordionTab, AccordionTab.name, `<${AccordionTab.name}></${AccordionTab.name}`],
    [AutoComplete, AutoComplete.name, `<${AutoComplete.name}></${AutoComplete.name}`],
    [Avatar, Avatar.name, `<${Avatar.name}></${Avatar.name}`],
    [AvatarGroup, AvatarGroup.name, `<${AvatarGroup.name}></${AvatarGroup.name}`],
    [Badge, Badge.name, `<${Badge.name}></${Badge.name}`],
    [BlockUI, BlockUI.name, `<${BlockUI.name}></${BlockUI.name}`],
    [Breadcrumb, Breadcrumb.name, `<${Breadcrumb.name}></${Breadcrumb.name}`],
    [Button, Button.name, `<${Button.name}></${Button.name}`],
    [ButtonGroup, ButtonGroup.name, `<${ButtonGroup.name}></${ButtonGroup.name}`],
    [Calendar, Calendar.name, `<${Calendar.name}></${Calendar.name}`],
    [Card, Card.name, `<${Card.name}></${Card.name}`],
    [Carousel, Carousel.name, `<${Carousel.name}></${Carousel.name}`],
    [CascadeSelect, CascadeSelect.name, `<${CascadeSelect.name}></${CascadeSelect.name}`],
    [Chart, Chart.name, `<${Chart.name}></${Chart.name}`],
    [Checkbox, Checkbox.name, `<${Checkbox.name}></${Checkbox.name}`],
    [Chip, Chip.name, `<${Chip.name}></${Chip.name}`],
    [Chips, Chips.name, `<${Chips.name}></${Chips.name}`],
    [ColorPicker, ColorPicker.name, `<${ColorPicker.name}></${ColorPicker.name}`],
    [Column, Column.name, `<${Column.name}></${Column.name}`],
    [ColumnGroup, ColumnGroup.name, `<${ColumnGroup.name}></${ColumnGroup.name}`],
    [ConfirmDialog, ConfirmDialog.name, `<${ConfirmDialog.name}></${ConfirmDialog.name}`],
    [ConfirmPopup, ConfirmPopup.name, `<${ConfirmPopup.name}></${ConfirmPopup.name}`],
    [ContextMenu, ContextMenu.name, `<${ContextMenu.name}></${ContextMenu.name}`],
    [DataTable, DataTable.name, `<${DataTable.name}></${DataTable.name}`],
    [DataView, DataView.name, `<${DataView.name}></${DataView.name}`],
    [DatePicker, DatePicker.name, `<${DatePicker.name}></${DatePicker.name}`],
    [DeferredContent, DeferredContent.name, `<${DeferredContent.name}></${DeferredContent.name}`],
    [Dialog, Dialog.name, `<${Dialog.name}></${Dialog.name}`],
    [Divider, Divider.name, `<${Divider.name}></${Divider.name}`],
    [Dock, Dock.name, `<${Dock.name}></${Dock.name}`],
    [Drawer, Drawer.name, `<${Drawer.name}></${Drawer.name}`],
    [Dropdown, Dropdown.name, `<${Dropdown.name}></${Dropdown.name}`],
    [DynamicDialog, DynamicDialog.name, `<${DynamicDialog.name}></${DynamicDialog.name}`],
    [Editor, Editor.name, `<${Editor.name}></${Editor.name}`],
    [Fieldset, Fieldset.name, `<${Fieldset.name}></${Fieldset.name}`],
    [FileUpload, FileUpload.name, `<${FileUpload.name}></${FileUpload.name}`],
    [FloatLabel, FloatLabel.name, `<${FloatLabel.name}></${FloatLabel.name}`],
    [Fluid, Fluid.name, `<${Fluid.name}></${Fluid.name}`],
    [Galleria, Galleria.name, `<${Galleria.name}></${Galleria.name}`],
    [IconField, IconField.name, `<${IconField.name}></${IconField.name}`],
    [IftaLabel, IftaLabel.name, `<${IftaLabel.name}></${IftaLabel.name}`],
    [Image, Image.name, `<${Image.name}></${Image.name}`],
    [ImageCompare, ImageCompare.name, `<${ImageCompare.name}></${ImageCompare.name}`],
    [InlineMessage, InlineMessage.name, `<${InlineMessage.name}></${InlineMessage.name}`],
    [Inplace, Inplace.name, `<${Inplace.name}></${Inplace.name}`],
    [InputChips, InputChips.name, `<${InputChips.name}></${InputChips.name}`],
    [InputGroup, InputGroup.name, `<${InputGroup.name}></${InputGroup.name}`],
    [InputGroupAddon, InputGroupAddon.name, `<${InputGroupAddon.name}></${InputGroupAddon.name}`],
    [InputIcon, InputIcon.name, `<${InputIcon.name}></${InputIcon.name}`],
    [InputMask, InputMask.name, `<${InputMask.name}></${InputMask.name}`],
    [InputNumber, InputNumber.name, `<${InputNumber.name}></${InputNumber.name}`],
    [InputOtp, InputOtp.name, `<${InputOtp.name}></${InputOtp.name}`],
    [InputSwitch, InputSwitch.name, `<${InputSwitch.name}></${InputSwitch.name}`],
    [InputText, InputText.name, `<${InputText.name}></${InputText.name}`],
    [Knob, Knob.name, `<${Knob.name}></${Knob.name}`],
    [ListBox, ListBox.name, `<${ListBox.name}></${ListBox.name}`],
    [MegaMenu, MegaMenu.name, `<${MegaMenu.name}></${MegaMenu.name}`],
    [Menu, Menu.name, `<${Menu.name}></${Menu.name}`],
    [Menubar, Menubar.name, `<${Menubar.name}></${Menubar.name}`],
    [Message, Message.name, `<${Message.name}></${Message.name}`],
    [MeterGroup, MeterGroup.name, `<${MeterGroup.name}></${MeterGroup.name}`],
    [MultiSelect, MultiSelect.name, `<${MultiSelect.name}></${MultiSelect.name}`],
    [OrderList, OrderList.name, `<${OrderList.name}></${OrderList.name}`],
    [OrganizationChart, OrganizationChart.name, `<${OrganizationChart.name}></${OrganizationChart.name}`],
    [OverlayBadge, OverlayBadge.name, `<${OverlayBadge.name}></${OverlayBadge.name}`],
    [OverlayPanel, OverlayPanel.name, `<${OverlayPanel.name}></${OverlayPanel.name}`],
    [Paginator, Paginator.name, `<${Paginator.name}></${Paginator.name}`],
    [Panel, Panel.name, `<${Panel.name}></${Panel.name}`],
    [PanelMenu, PanelMenu.name, `<${PanelMenu.name}></${PanelMenu.name}`],
    [Password, Password.name, `<${Password.name}></${Password.name}`],
    [PickList, PickList.name, `<${PickList.name}></${PickList.name}`],
    [Popover, Popover.name, `<${Popover.name}></${Popover.name}`],
    [ProgressBar, ProgressBar.name, `<${ProgressBar.name}></${ProgressBar.name}`],
    [ProgressSpinner, ProgressSpinner.name, `<${ProgressSpinner.name}></${ProgressSpinner.name}`],
    [RadioButton, RadioButton.name, `<${RadioButton.name}></${RadioButton.name}`],
    [Rating, Rating.name, `<${Rating.name}></${Rating.name}`],
    [Row, Row.name, `<${Row.name}></${Row.name}`],
    [ScrollPanel, ScrollPanel.name, `<${ScrollPanel.name}></${ScrollPanel.name}`],
    [ScrollTop, ScrollTop.name, `<${ScrollTop.name}></${ScrollTop.name}`],
    [Select, Select.name, `<${Select.name}></${Select.name}`],
    [SelectButton, SelectButton.name, `<${SelectButton.name}></${SelectButton.name}`],
    [Sidebar, Sidebar.name, `<${Sidebar.name}></${Sidebar.name}`],
    [Skeleton, Skeleton.name, `<${Skeleton.name}></${Skeleton.name}`],
    [Slider, Slider.name, `<${Slider.name}></${Slider.name}`],
    [SpeedDial, SpeedDial.name, `<${SpeedDial.name}></${SpeedDial.name}`],
    [SplitButton, SplitButton.name, `<${SplitButton.name}></${SplitButton.name}`],
    [Splitter, Splitter.name, `<${Splitter.name}></${Splitter.name}`],
    [SplitterPanel, SplitterPanel.name, `<${SplitterPanel.name}></${SplitterPanel.name}`],
    [Step, Step.name, `<${Step.name}></${Step.name}`],
    [StepItem, StepItem.name, `<${StepItem.name}></${StepItem.name}`],
    [StepList, StepList.name, `<${StepList.name}></${StepList.name}`],
    [StepPanel, StepPanel.name, `<${StepPanel.name}></${StepPanel.name}`],
    [StepPanels, StepPanels.name, `<${StepPanels.name}></${StepPanels.name}`],
    [Stepper, Stepper.name, `<${Stepper.name}></${Stepper.name}`],
    [Steps, Steps.name, `<${Steps.name}></${Steps.name}`],
    [Tab, Tab.name, `<${Tab.name}></${Tab.name}`],
    [TabList, TabList.name, `<${TabList.name}></${TabList.name}`],
    [TabMenu, TabMenu.name, `<${TabMenu.name}></${TabMenu.name}`],
    [TabPanel, TabPanel.name, `<${TabPanel.name}></${TabPanel.name}`],
    [TabPanels, TabPanels.name, `<${TabPanels.name}></${TabPanels.name}`],
    [Tabs, Tabs.name, `<${Tabs.name}></${Tabs.name}`],
    [Tag, Tag.name, `<${Tag.name}></${Tag.name}`],
    [Terminal, Terminal.name, `<${Terminal.name}></${Terminal.name}`],
    [Textarea, Textarea.name, `<${Textarea.name}></${Textarea.name}`],
    [TieredMenu, TieredMenu.name, `<${TieredMenu.name}></${TieredMenu.name}`],
    [Timeline, Timeline.name, `<${Timeline.name}></${Timeline.name}`],
    [Toast, Toast.name, `<${Toast.name}></${Toast.name}`],
    [ToggleButton, ToggleButton.name, `<${ToggleButton.name}></${ToggleButton.name}`],
    [ToggleSwitch, ToggleSwitch.name, `<${ToggleSwitch.name}></${ToggleSwitch.name}`],
    [Toolbar, Toolbar.name, `<${Toolbar.name}></${Toolbar.name}`],
    [Tree, Tree.name, `<${Tree.name}></${Tree.name}`],
    [TreeSelect, TreeSelect.name, `<${TreeSelect.name}></${TreeSelect.name}`],
    [TreeTable, TreeTable.name, `<${TreeTable.name}></${TreeTable.name}`],
    [VirtualScroller, VirtualScroller.name, `<${VirtualScroller.name}></${VirtualScroller.name}`],
  ]
}
